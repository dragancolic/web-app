import { SeverityType } from "./SeverityType";

describe('Severity Type Test Suite', () => {

    it ('should check if severity type exists', () => {
        expect(SeverityType).not.toBe(undefined);
    });
});